# Fix 01: CI 集成测试端口不匹配修复 Implementation Plan

> **For agentic workers:** 独立方案，在独立 worktree/分支执行（`git worktree add ../agent-studio-p1 main -b fix/p1-port`）。只修改本文件列出的文件，禁止改动其它方案的独占文件。

**Goal:** 让 `integration` 门禁真正跑起来，不再因端口探测被静默 skip。
**Architecture:** 把 conftest 的 `BASE` 改为环境变量可覆盖（默认保持 8082 兼容本地），CI 集成 job 通过 job 级 env 注入 8080。
**Tech Stack:** pytest / GitHub Actions

## 并行协调

- 独占文件：`backend/tests/conftest.py`、`.github/workflows/ci.yml`（integration job 顶部，~L400）
- ci.yml 共享：与 P8(L449)/P2(L537)/P6(L183-195) 行区间不重叠；**merge 顺序 P1→P8→P2→P6**

## Global Constraints

- 不修改其它方案的独占文件
- 提交遵循 .gitmessage 格式

---

## 根因

`backend/tests/conftest.py:69` 硬编码 `BASE="http://localhost:8082"`，CI 在 8080 起后端（`ci.yml:438`）→ conftest 的 skip hook（~L302-312）探测 8082 不通 → 全部 integration 测试被 `pytest.skip`，pytest 以 0 退出 → `ci-passed` 门禁对 skipped 放行，集成门禁形同虚设。

## Files

- Modify: `backend/tests/conftest.py:69`
- Modify: `.github/workflows/ci.yml`（integration job 顶部，~L394-400 后插入 job 级 `env:`）

---

- [ ] **Step 1: conftest 改为环境变量可覆盖**

```python
BASE = os.environ.get("E2E_BASE_URL", "http://localhost:8082")
```

- [ ] **Step 2: ci.yml integration job 顶部加 job 级 env**

在 integration job 的 `strategy:` 块之后插入（保持 8080 与下方 uvicorn 启动一致）：

```yaml
    env:
      E2E_BASE_URL: http://localhost:8080
```

- [ ] **Step 3: 本地验证**

```bash
# 本地起 8082 后端后：
pytest backend/tests/e2e/ -m integration -v 2>&1 | tail -5
# 期望：测试真正执行（非 skipped），PASSED
```

- [ ] **Step 4: Commit**

```bash
git add backend/tests/conftest.py .github/workflows/ci.yml
git commit -m "fix(ci): make E2E_BASE_URL env-overridable so integration gate runs on 8080"
```

## Self-Review

- 本地默认 8082 行为不变（`os.environ.get` 缺省值保留）
- CI 显式传 8080，与 uvicorn 启动端口一致
- ci.yml 编辑区域为 integration job 顶部，与 P8/P2/P6 不重叠
