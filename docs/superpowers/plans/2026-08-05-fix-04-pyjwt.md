# Fix 04: 手写 JWT 迁移到 PyJWT Implementation Plan

> **For agentic workers:** 独立方案，在独立 worktree/分支执行（`git worktree add ../agent-studio-p4 main -b fix/p4-pyjwt`）。只修改本文件列出的文件。本地开发先 `pip install pyjwt`；requirements.txt 的 pin 由 P14 落地。

**Goal:** 用 PyJWT 替换自研 HMAC JWT，加 `alg` 校验；保留 legacy simplified token 解码兼容。
**Architecture:** `decode_jwt`/`create_token` 对外签名不变；标准 JWT 走 PyJWT（HS256 + header alg 强制校验），遗留 `base64(user_id:ts:sig[:16])` 简化格式保留独立解码分支。
**Tech Stack:** Python 3.12 / PyJWT

## 并行协调

- 独占文件：`backend/src/auth/auth_jwt.py`、`backend/tests/auth/test_auth_jwt.py`
- **依赖 P14**：`pyjwt==2.10.1` 由 P14 写入 requirements.txt；本方案不碰 requirements.txt，**P14 先 merge**
- 调用方（auth_middleware.py / auth_rbac.py）签名不变，无需改动

## Global Constraints

- 不修改其它方案的独占文件
- 保持对外 API：`decode_jwt(token: str, secret: str) -> dict | None`、`create_token(user_id, secret, ttl=86400) -> str`
- 提交遵循 .gitmessage 格式

---

## 根因

自研 JWT（`auth_jwt.py`）不校验 header 的 `alg` 字段（恒按 HS256 验签）、无 `iss/aud/nbf` 校验；简化 token 签名截断为 16 位 hex（64bit）强度不足。PyJWT 为社区标准实现（Python JWT 事实标准库）。

## Files

- Modify: `backend/src/auth/auth_jwt.py`
- Modify: `backend/tests/auth/test_auth_jwt.py`

---

- [ ] **Step 1: 重写 auth_jwt.py（保留对外 API）**

```python
from typing import Any, cast

"""JWT token creation and verification via PyJWT (HS256).

Keeps a legacy decode branch for the pre-PyJWT simplified HMAC token format
``base64(user_id:ts:sig[:16])`` so previously issued tokens still validate.
"""

import base64
import hashlib
import hmac
import os
import time

import jwt

from core.infra.logging_config import get_logger

logger = get_logger(__name__)

AUTH_SECRET = os.environ.get("AUTH_SECRET", "")


def _base64url_decode(data: str) -> bytes:
    """Decode base64url-encoded string with padding fix."""
    rem = len(data) % 4
    if rem:
        data += "=" * (4 - rem)
    return base64.urlsafe_b64decode(data)


def decode_jwt(token: str, secret: str) -> dict[str, Any] | None:
    """Decode and verify a JWT token.

    Returns the payload dict if valid, None otherwise.
    Standard tokens are verified with PyJWT (HS256, header alg enforced).
    Simplified legacy tokens fall back to the old HMAC verification.
    """
    if not secret:
        return None

    parts = token.split(".")
    if len(parts) == 3:
        try:
            payload = jwt.decode(
                token,
                secret,
                algorithms=["HS256"],
                options={"require": ["exp"], "verify_exp": True},
            )
            return cast(dict[str, Any], payload)
        except Exception:
            logger.warning("JWT decode error", exc_info=True)
            return None

    # Legacy simplified token: base64url(user_id:ts:sig[:16])
    if len(parts) == 1:
        try:
            raw = _base64url_decode(token).decode()
            user_id, ts_str, provided_sig = raw.rsplit(":", 2)
            expected = hmac.new(
                secret.encode(),
                f"{user_id}:{ts_str}".encode(),
                hashlib.sha256,
            ).hexdigest()[:16]
            if not hmac.compare_digest(provided_sig, expected):
                return None
            if int(ts_str) < int(time.time()) - 86400:
                return None
            return {"sub": user_id, "iat": int(ts_str)}
        except (ValueError, UnicodeDecodeError):
            return None

    return None


def create_token(user_id: str, secret: str, ttl: int = 86400) -> str:
    """Create an HS256 JWT token for the given user_id."""
    now = int(time.time())
    return jwt.encode(
        {"sub": user_id, "iat": now, "exp": now + ttl},
        secret,
        algorithm="HS256",
    )
```

- [ ] **Step 2: 运行现有 JWT 测试**

Run: `pytest backend/tests/auth/test_auth_jwt.py -v`
Expected: 全 PASS（行为保持：有效/过期/错密钥/篡改/简化 token）

- [ ] **Step 3: 新增 alg 伪造测试**

```python
def test_rejects_header_alg_forgery():
    from auth.auth_jwt import create_token, AUTH_SECRET, decode_jwt
    tok = create_token("u1", AUTH_SECRET)
    header, payload, sig = tok.split(".")
    import base64, json
    forged_header = base64.urlsafe_b64encode(
        json.dumps({"alg": "none", "typ": "JWT"}).encode()).rstrip(b"=").decode()
    forged = f"{forged_header}.{payload}.{sig}"
    assert decode_jwt(forged, AUTH_SECRET) is None
```

Run: `pytest backend/tests/auth/test_auth_jwt.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add backend/src/auth/auth_jwt.py backend/tests/auth/test_auth_jwt.py
git commit -m "security(auth): replace hand-rolled JWT with PyJWT + alg verification"
```

## Self-Review

- 对外签名不变，auth_middleware/auth_rbac 零改动
- PyJWT `algorithms=["HS256"]` 强制 header alg 校验，`alg:none` 伪造被拒
- legacy simplified 解码保留，既有 token 不受影响
- pyjwt pin 由 P14 落地（`pyjwt==2.10.1`）
