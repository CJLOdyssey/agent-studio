# Fix 08: 全部后端 E2E 纳入 CI Implementation Plan

> **For agentic workers:** 独立方案，在独立 worktree/分支执行（`git worktree add ../agent-studio-p8 main -b fix/p8-full-e2e`）。只修改本文件列出的文件。

**Goal:** CI 集成 job 从只跑 31 个测试扩展到 `backend/tests/e2e/` 全部（81 个，含 auth/run/workflow/websocket/mcp）。
**Architecture:** 集成 job 的 pytest 命令从 `test_e2e_full_flow.py` 改为整个 `backend/tests/e2e/` 目录。
**Tech Stack:** pytest / GitHub Actions

## 并行协调

- 独占文件：`.github/workflows/ci.yml`（integration job "Run E2E tests" 步骤，~L449）
- ci.yml 共享：编辑 L449 命令行；**依赖 P1 的 `E2E_BASE_URL` env（L394-400）**；**merge 顺序 P1→P8→P2→P6（本方案第二）**

## Global Constraints

- 不修改其它方案的独占文件（conftest.py 属 P1，本方案只改 ci.yml 命令）
- 提交遵循 .gitmessage 格式

---

## 根因

`ci.yml:449` 只跑 `backend/tests/e2e/test_e2e_full_flow.py`（re-export 8 个类，31 个测试）；`test_auth_flow`(16)/`test_run_flow`(18)/`test_workflow_crud`(10)/`test_websocket_stream`(2)/`test_mcp_invocation`(1)/`test_integration_rbac_checkpoint`(3) 共 50 个覆盖登录/运行/工作流/WS/MCP 核心路径的测试从不执行。

## Files

- Modify: `.github/workflows/ci.yml`（integration job "Run E2E tests"，~L449）

---

- [ ] **Step 1: 替换集成 job 的 pytest 命令**

```yaml
      - name: Run E2E tests
        run: PYTHONPATH=. AUTH_MODE=${{ matrix.auth_mode }} CHECKPOINTER_BACKEND=memory RATE_LIMIT=9999 REDIS_URL=redis://localhost:6379/1 AUTH_SECRET=ci-test-secret python3 -m pytest backend/tests/e2e/ -v --tb=short --reruns 2 --reruns-delay 5
```

（依赖 P1 的 `E2E_BASE_URL` 已指向 8080。）

- [ ] **Step 2: 本地验证**

```bash
E2E_BASE_URL=http://localhost:8082 pytest backend/tests/e2e/ -m integration -v --tb=short 2>&1 | tail -5
# 期望：81 个全部执行（非 skipped），PASSED
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: run full backend E2E suite (81 tests) in integration job"
```

## Self-Review

- 保留 `--reruns 2 --reruns-delay 5` 容忍偶发 flaky
- 两个 auth_mode（legacy/rbac）矩阵继续生效
- 若个别 e2e 文件在 CI 环境不稳，先在本分支内修复该测试，不缩小范围
