# Fix 03: X-User-ID 身份伪造修复 Implementation Plan

> **For agentic workers:** 独立方案，在独立 worktree/分支执行（`git worktree add ../agent-studio-p3 main -b fix/p3-x-user-id`）。只修改本文件列出的文件。

**Goal:** 受保护模式下 `get_user_id` 不再信任客户端可控的 `X-User-ID` 头。
**Architecture:** `get_user_id()` 仅在 auth 未启用（legacy/guest 模式，此时 X-User-ID 是访客数据命名空间而非安全边界）时保留 header 兜底；`AUTH_ENABLED` 时未认证请求一律解析为 `anonymous` 并记 WARN。
**Tech Stack:** Python 3.12 / FastAPI

## 并行协调

- 独占文件：`backend/src/auth/auth_rbac.py`、`backend/tests/auth/test_auth_rbac.py`
- 无共享文件

## Global Constraints

- 不修改其它方案的独占文件（routers/ 下的所有权调用点由本项目 get_user_id 一处收敛修复）
- TDD：先写失败测试再实现
- 提交遵循 .gitmessage 格式

---

## 根因

`get_user_id()`（`backend/src/auth/auth_rbac.py:156-174`）兜底优先级：`request.state.user_id` → JWT cookie → **`X-User-ID` 请求头** → `'anonymous'`。该头完全由客户端控制、无签名；而 `AuthMiddleware`（`auth_middleware.py:44-46`）无 token 时放行标记 `is_authenticated=False`。RBAC/AUTH_ENABLED 模式下 keys/teams/runs/models 的所有权判断（`get_user_id()` 31 处调用）可被未认证请求伪造。legacy 模式下该头是访客数据命名空间（前端 `agentstudio_user_id` localStorage），非安全边界，需保留。

## Files

- Modify: `backend/src/auth/auth_rbac.py`（`get_user_id`，L156-174）
- Modify: `backend/tests/auth/test_auth_rbac.py`

---

- [ ] **Step 1: 写失败测试**

在 `backend/tests/auth/test_auth_rbac.py` 增加：

```python
import os
from unittest.mock import MagicMock
from auth.auth_rbac import get_user_id

def test_get_user_id_ignores_x_user_id_when_auth_enabled(monkeypatch):
    monkeypatch.setenv("AUTH_ENABLED", "1")
    # AUTH_ENABLED 在 import 时读取，需要直接改模块属性
    import auth.auth_rbac as ar
    old = ar.AUTH_ENABLED
    ar.AUTH_ENABLED = True
    try:
        req = MagicMock()
        req.state.user_id = None
        req.cookies.get.return_value = None
        req.headers.get.return_value = "victim-id"
        assert get_user_id(req) == "anonymous"
    finally:
        ar.AUTH_ENABLED = old
        monkeypatch.undo()
```

- [ ] **Step 2: 验证失败**

Run: `pytest backend/tests/auth/test_auth_rbac.py::test_get_user_id_ignores_x_user_id_when_auth_enabled -v`
Expected: FAIL（当前返回 `"victim-id"`）

- [ ] **Step 3: 修复 get_user_id**

```python
def get_user_id(request: Any) -> str:
    """Extract user identity from the authenticated request.

    Priority: auth middleware (request.state.user_id) → JWT cookie → 'anonymous'.
    The ``X-User-ID`` header is ONLY trusted in legacy/guest mode (auth disabled),
    where it is a guest-data namespace, NOT a security boundary. When auth is
    enabled, unauthenticated requests resolve to ``anonymous``.
    """
    user_id: str | None = getattr(request.state, "user_id", None)
    if user_id:
        return user_id

    # Check httpOnly access_token cookie (set by login/register/verify/refresh endpoints)
    token = request.cookies.get("access_token")
    if token:
        payload = decode_jwt(token, AUTH_SECRET)
        if payload:
            uid = payload.get("sub")
            if isinstance(uid, str) and uid:
                return uid

    if AUTH_ENABLED:
        logger.warning("Unauthenticated ownership access | path=%s", request.url.path)
        return "anonymous"

    return str(request.headers.get("X-User-ID", "anonymous"))
```

- [ ] **Step 4: 验证通过**

Run: `pytest backend/tests/auth/test_auth_rbac.py -v`
Expected: 新测试 PASS，原有 auth 测试全绿

- [ ] **Step 5: Commit**

```bash
git add backend/src/auth/auth_rbac.py backend/tests/auth/test_auth_rbac.py
git commit -m "security(auth): stop trusting X-User-ID header when auth enabled"
```

## Self-Review

- legacy 默认模式（AUTH_ENABLED=0）行为不变，访客命名空间保留
- rbac 模式（AUTH_ENABLED=1）伪造头被拒，落到 `anonymous`
- `AUTH_ENABLED` 为该模块既有常量，直接复用
