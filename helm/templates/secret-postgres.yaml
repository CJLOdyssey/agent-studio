{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "virtual-team.fullname" . }}-postgres
  labels:
    {{- include "virtual-team.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.postgres.password | required "postgres.password is required when postgres.enabled is true" | quote }}
{{- end }}
